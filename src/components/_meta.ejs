<%
    <!-- Sample Code -->
    site = {
        url: 'https://example.com',
        title: 'Title Sample',
        description: 'Description Text'
    };
%>

<% if(page.id == 'index') { %>
    <%
        site = {
            url: 'https://example.com',
            title: 'Top | Sample Sites',
            description: 'Description Text'
        };
    %>
<% } else if (page.id == 'hoge') { %>
    <%
        site = {
            url: 'https://example.com/hoge/',
            title: 'Hoge | Sample Sites',
            description: 'Description Text'
        };
    %>
<% } else if (page.id == 'news') { %>
    <%
        site = {
            url: 'https://example.com/news/',
            title: 'News | Sample Sites',
            description: 'Description Text'
        };
    %>
<% } %>
